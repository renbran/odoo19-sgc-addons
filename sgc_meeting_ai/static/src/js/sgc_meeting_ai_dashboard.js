// SGC Meeting AI — Dashboard widgets.
// Currently no JS extension needed; backend uses standard widgets.
odoo.define("sgc_meeting_ai.dashboard", ["web.core"], function (require) {
    "use strict";
    var Widget = require("web.Widget");
    return Widget.extend({
        // Placeholder for future dashboard widgets
    });
});
