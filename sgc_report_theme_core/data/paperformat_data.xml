<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="0">
<!--
    New, additive paperformat records. Nothing existing is modified or
    reassigned - a bridge's report action must explicitly set
    paperformat_id to one of these for it to take effect on that report.
-->

    <!--
        Brand Guidelines v3.0 (Chapter 05 - Page Architecture) specifies
        20mm margins on all sides for both portrait and landscape. Left/
        right corrected here to 20mm (were 12mm/14mm). margin_top/bottom
        are left as previously set: they interact with header_spacing in
        wkhtmltopdf's own layout model (the header/footer running content
        needs room, not just a pure design margin), and this paperformat
        is currently unused by any bridge - sgc_report_theme_default ships
        its own paperformat_sgc_documents, tuned from a real measured
        header/title overlap defect. Verify top/bottom empirically with an
        actual PDF render before wiring this paperformat to a report,
        rather than assuming 20mm top/bottom is safe untested (see the
        report framework plan's Responsive Print Rules, §13).
    -->
    <record id="paperformat_sgc_a4_portrait" model="report.paperformat">
        <field name="name">SGC A4 Portrait</field>
        <field name="default" eval="False"/>
        <field name="format">A4</field>
        <field name="orientation">Portrait</field>
        <field name="margin_top">28</field>
        <field name="margin_bottom">22</field>
        <!-- 2026-08-02 Option B full-bleed: left/right margins zeroed so the
             wkhtmltopdf body box reaches the paper's side edges and the
             article's ivory background paints the side bands edge-to-edge.
             The 20mm brand inset is restored as padding on the article via
             sgc_report.scss.              Top/bottom margins stay: the native
             header-html/footer-html stamp the top/bottom margin zones
             and carry their own ivory background (html/body ivory in the
             report assets), so all four bands read full-bleed ivory. -->
        <field name="margin_left">0</field>
        <field name="margin_right">0</field>
        <field name="header_line" eval="False"/>
        <!-- 2026-08-02 Option B: header_spacing MUST be 0.
             wkhtmltopdf gives the header stamp a slot of
             (margin_top - header_spacing), so 24 left only 4mm of the 28mm
             band for a header whose content needs the full 28mm - all that
             survived on the page was the last address line and the gold rule,
             which is what made the band read as "blank" for so long.
             0 hands the header the whole 28mm band, and sgc_report.scss pins
             .o_sgc_header to exactly 28mm so it fills that slot without
             overflowing it.

             This value is also why the bug kept coming back: the fix had only
             ever been written to the DB with an ORM write, while this file
             still said 24, so every -u sgc_report_theme_core silently reverted
             it. Change it HERE, not in the database. -->
        <field name="header_spacing">0</field>
        <field name="dpi">96</field>
    </record>

    <record id="paperformat_sgc_a4_landscape" model="report.paperformat">
        <field name="name">SGC A4 Landscape</field>
        <field name="default" eval="False"/>
        <field name="format">A4</field>
        <field name="orientation">Landscape</field>
        <field name="margin_top">20</field>
        <field name="margin_bottom">18</field>
        <field name="margin_left">20</field>
        <field name="margin_right">20</field>
        <field name="header_line" eval="False"/>
        <field name="header_spacing">20</field>
        <field name="dpi">96</field>
    </record>

</odoo>
